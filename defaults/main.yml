---
docker_compose_path: '/usr/local/bin/docker-compose'

remote_deploy_base_path: '/srv/docker'
remote_deploy_name: 'docker-stack'

remote_pull_images: false
remote_remove_only: false
remote_remove_args: '--rmi all --remove-orphans'

#remote_docker_login_registry:
#remote_docker_login_user:
#remote_docker_login_pass:

deploy_path: '.'

force_recreation: false
